/**
 * Google Maps API configuration constants
 * These constants prevent LoadScript from recreating libraries on every render
 */

import { Libraries } from '@react-google-maps/api'

export const GOOGLE_LIBRARIES: Libraries = ['places']


